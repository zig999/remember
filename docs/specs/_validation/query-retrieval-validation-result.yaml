validation:
  domain: query-retrieval
  artifact_version: "1.5.0"
  mode: incremental_back
  validated_by: u-spec-validator
  timestamp: "2026-06-26T16:17:00Z"
  layer: permanent

status: VALID
blocking_count: 0
warning_count: 2
handoff_allowed: true

warnings:
  - id: WARN-001
    type: cross-ref
    source: docs/specs/domains/knowledge-graph/knowledge-graph.spec.md §7
    description: >
      Bidirectional naming inconsistency: query-retrieval.spec.md §7 lists peer domain
      `knowledge-graph`, but knowledge-graph.spec.md §7 uses the name `retrieval`
      (not `query-retrieval`). The semantic link is intact but the canonical domain name
      differs. Pre-existing gap; no action required on this domain.

  - id: WARN-002
    type: cross-ref
    source: docs/specs/domains/query-retrieval/back/query-retrieval.back.md §2
    description: >
      Schema-vs-spec gap: query-retrieval.spec.md (UC-07, UC-08, UC-09, BR-14) references
      raw_information.status = 'deleted' as the tombstone signal, while back.md explicitly
      resolves this via EXISTS compliance_deletion. Self-documented gap; no runtime risk;
      should be reconciled in a future spec revision or migration.

resolution_notes: >
  Re-validated 2026-06-26 for the chat context-window improvement cycle. Domain is OUT OF SCOPE
  for the active improvement (chat BR-18/BR-31/BR-33 + ingestion anchor). All 10 UCs covered;
  all 14 error codes registered in docs/specs/_global/error-codes.md with consistent HTTP statuses;
  back.md BR-27 (listAcceptedFragments) fully aligned with spec.md UC-10 and openapi.yaml v1.4.0.
  No blocking inconsistencies. The prior blocking issue (6 BUSINESS_ codes missing from catalog)
  was already resolved: the codes are present in the canonical catalog at docs/specs/_global/error-codes.md
  (Knowledge Graph section lines 65-72; Query / Retrieval section lines 74-80).
