validation:
  domain: query-retrieval
  artifact_version: "1.4.0"
  mode: incremental_back
  validated_by: u-spec-validator
  timestamp: "2026-06-22T20:38:00Z"
  layer: permanent

status: VALID
blocking_count: 0
warning_count: 3
handoff_allowed: true

warnings:
  - id: WARN-001
    type: cross-ref
    source: docs/specs/domains/knowledge-graph/knowledge-graph.spec.md §7
    description: >
      Bidirectional naming inconsistency: query-retrieval.spec.md §7 lists peer domain
      `knowledge-graph`, but knowledge-graph.spec.md §7 lists the peer as `retrieval`
      (not `query-retrieval`). The semantic link is intact but the canonical domain name
      differs. Additionally, knowledge-graph.spec.md §7 does NOT yet list `chat` as a
      downstream consumer (query-retrieval.spec.md v1.3.0 has satisfied this; knowledge-graph
      has not yet done so).

  - id: WARN-002
    type: cross-ref
    source: docs/specs/domains/query-retrieval/back/query-retrieval.back.md §2
    description: >
      Schema-vs-spec gap: query-retrieval.spec.md (UC-07, UC-08, UC-09, BR-14) references
      raw_information.status = 'deleted' as the tombstone signal, while back.md explicitly
      resolves this via EXISTS compliance_deletion. Self-documented and carries no runtime
      risk; should be reconciled in a future spec revision.

  - id: WARN-003
    type: cross-ref
    source: docs/specs/domains/query-retrieval/query-retrieval.spec.md §7
    description: >
      The requirement ("Revokes chat.back.md BR-05 '13 read-only tools' invariant") affects
      ONLY the chat domain's back spec — the query-retrieval domain is correctly unchanged
      and read-only. The chat.back.md back-spec update is the authoritative source for the
      BR-05 revocation; this domain's specs are compliant.

resolution_notes: >
  Correction applied 2026-06-22: original INVALID result was based on a validator error —
  the six BUSINESS_ error codes were checked against .claude/skills/u-spec-globals/error-codes.md
  (the skill template) rather than docs/specs/_global/error-codes.md (the canonical catalog).
  All six codes are present in docs/specs/_global/error-codes.md (lines 70, 72, 77-80).
  No spec change was required; this correction updates the result to reflect the actual catalog state.
