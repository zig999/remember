validation:
  domain: curation
  artifact_version: "1.1.0"
  mode: incremental_back
  validated_by: u-spec-validator
  timestamp: "2026-06-26T16:02:45Z"
  layer: permanent

status: VALID
blocking_count: 0
warning_count: 3
handoff_allowed: true

blocking_issues: []

warnings:
  - id: WARN-001
    type: cross-ref
    source: "docs/specs/domains/curation/openapi.yaml correctItem 409 response"
    description: >
      The correctItem 409 example reuses BUSINESS_ITEM_NOT_DELETABLE for a state that is
      semantically about correctability, not deletability. The code is registered and
      consistent across all files, but dual-use may confuse callers. A distinct
      BUSINESS_ITEM_NOT_CORRECTABLE code would reduce ambiguity. No action required for
      handoff.
  - id: WARN-002
    type: cross-ref
    source: "docs/specs/domains/curation/back/curation.back.md §3 BR-26"
    description: >
      BR-26 lists UC-02, UC-04, UC-05, UC-06, UC-10 for SELECT...FOR UPDATE but omits
      UC-03 (resolveEntityMatch keep_separate), which also mutates knowledge_node.status.
      The state-machine guard BR-22 implicitly requires the lock; BR-26's UC list is
      incomplete documentation but no functional gap exists.
  - id: WARN-003
    type: cross-ref
    source: "docs/specs/domains/curation/curation.spec.md §7 Cross-Domain Dependencies"
    description: >
      The 'auth' entry in the dependencies table is framed as a peer domain with a
      bidirectionality claim, but Neon Auth is infrastructure (no auth.spec.md peer
      domain exists). Accurate in intent but may trigger a false positive in future
      bidirectionality sweeps.
