validation:
  domain: front-global
  artifact_version: "1.0.0"
  mode: final_front
  validated_by: u-spec-validator
  timestamp: "2026-06-18T23:59:31Z"
  layer: permanent

status: VALID
blocking_count: 0
warning_count: 3
handoff_allowed: true

blocking_issues: []

warnings:
  - id: WARN-001
    type: design-system
    source: docs/specs/front/design-system/_index.md
    description: >
      composition.md, components.md, and implementation.md are absent from design-system/.
      Explicitly declared out-of-scope for this foundation wave by _index.md §3.
      Must be produced before any feature spec references them.

  - id: WARN-002
    type: design-system
    source: docs/specs/front/
    description: >
      design-system-rules.md does not exist. Not listed as deferred in _index.md.
      Required by validator rule 12b once design-system tokens are finalized.
      Responsible: Front Spec Agent.

  - id: WARN-003
    type: cross-ref
    source: docs/specs/front/front.back.md §5 (EV-01 to EV-04)
    description: >
      EV payload tokens blocks reference non-existent token names:
      --motion-duration-pulse, --motion-easing-pulse, --motion-duration-promote,
      --motion-easing-promote, --motion-duration-supersede, --motion-easing-supersede,
      --motion-duration-merge, --motion-easing-merge.
      Canonical token names in tokens.md §11 are --duration-* and --ease-*
      (e.g. --duration-pulse, --ease-in-out). EV payloads are illustrative but
      misaligned names could confuse implementors.
      Suggested fix: update EV payload tokens blocks to use canonical names,
      or add a note that tokens in payloads are logical identifiers mapped in lib/motion.ts.
      Responsible: Back Spec Agent.
