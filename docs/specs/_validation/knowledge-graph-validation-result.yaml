validation:
  domain: knowledge-graph
  artifact_version: "1.5.0"
  mode: incremental_back
  validated_by: u-spec-validator
  timestamp: "2026-06-26T16:14:00Z"
  layer: permanent
  scope_note: >
    knowledge-graph is OUT OF SCOPE for the current improvement (chat temporal-context
    + ingestion received_at anchor). Validation confirms no spec changes needed.
    Spec files validated: knowledge-graph.spec.md v1.2.0, knowledge-graph.back.md v1.5.0,
    openapi.yaml v1.5.0.

status: VALID
blocking_count: 0
warning_count: 1
handoff_allowed: true

blocking_issues: []

warnings:
  - id: WARN-001
    type: coverage
    source: docs/specs/domains/knowledge-graph/back/knowledge-graph.back.md §5 Domain Events
    description: >
      No domain events (EV) are declared. The spec explicitly states
      "N/A -- no domain events in this version." This domain is READ-ONLY;
      it observes writes by ingestion/curation/compliance purely through
      the database. This is informational: per the skill rules, an EV without
      a declared consumer is warning-level only. The absence of an event
      section is consistent with the normative architecture (§2.2 "store único";
      no event bus). No action required.
