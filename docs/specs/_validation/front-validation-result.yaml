validation:
  domain: front
  artifact_version: "2.0.0"
  mode: final_front
  validated_by: u-spec-validator
  timestamp: "2026-06-21T01:06:44Z"
  layer: permanent

status: VALID
blocking_count: 0
warning_count: 2
handoff_allowed: true

blocking_issues: []

warnings:
  - id: WARN-001
    type: cross-ref
    source: docs/specs/front/_flows/auth.flow.md §3 state-transition-table
    description: >
      State transition table row for "/sign-in UI-01 → Form submitted → SDK call in flight"
      contains the phrase "SDK call in flight" — this is a vestigial Stack Auth SDK label.
      The rest of the flow consistently uses "Better Auth" and "2-step" language.
      Not blocking because the intent (UI-02 submitting state) is clear and the behavioral
      description is correct; however the label "SDK call in flight" should be updated to
      "Better Auth 2-step call in flight" for strict consistency.
    responsible: u-spec-front

  - id: WARN-002
    type: coverage
    source: docs/specs/front/_flows/auth.flow.md §1 (Involved Features)
    description: >
      The flow lists /chat as an involved feature with spec features/chat.feature.spec.md,
      which exists. However the chat feature spec is not being modified in this improvement wave
      and is not in scope for this validation. Noting this as coverage information only —
      the reference is structurally valid and the file exists.
    responsible: u-spec-front
