validation:
  domain: ingestion
  artifact_version: "1.6.1"
  mode: final_complete
  validated_by: u-spec-validator
  timestamp: "2026-07-03T03:17:00Z"
  layer: permanent

status: VALID
blocking_count: 0
warning_count: 2
handoff_allowed: true

blocking_issues: []

warnings:
  - id: WARN-001
    type: cross-ref
    source: "docs/specs/domains/ingestion/back/ingestion.back.md"
    description: >
      W-01 (carry-over): DEFAULT_PROMPT_VERSION declared as 'v2' in back.md §1 Anthropic client
      config row, but openapi.yaml info description references extraction.v3 as the registry default
      since v1.4.0. The two documents are internally consistent for P2.1 (error-code taxonomy only);
      this mismatch predates P2.1 and does not block handoff for this repair cycle.
  - id: WARN-002
    type: cross-ref
    source: "docs/specs/domains/ingestion/openapi.yaml"
    description: >
      W-02 (carry-over): openapi.yaml info.description mentions v1.4.2 as the version that
      introduced extraction prompt v3 and the received_at date-baseline feature. The back.md §1
      Anthropic client config row still declares DEFAULT_PROMPT_VERSION = 'v2' and ships
      extraction.v1.ts / extraction.v2.ts with no mention of v3. This editorial drift does not
      affect error-code taxonomy correctness and does not block P2.1 handoff.
