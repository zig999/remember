validation:
  domain: "graph-NodeDetailPanel-progressive-disclosure"
  artifact_version: "2.0.0"
  mode: "final_front"
  validated_by: "u-spec-validator"
  timestamp: "2026-06-26T23:25:30Z"
  layer: "permanent"

status: VALID
blocking_count: 0
warning_count: 2
handoff_allowed: true

blocking_issues: []

warnings:
  - id: "WARN-001"
    type: "component-gap"
    source: "docs/specs/front/features/chat.feature.spec.md §7"
    description: "NodeDetailPanel, NodeAttributeRow, NodeRelationshipRow, NodeProvenanceChain are referenced in §10 (components created/updated) and §11 (UC-CG-09) of chat.feature.spec.md but are NOT listed in the §7 'Shared Components Used' table. This is expected by convention (feature-local components do not go in §7) but the component spec does qualify (complex logic + sub-components), and the §10 table entry for NodeDetailPanel still shows the old scope description from v1.1.0 ('fetches getNodeById, shows aliases + current attributes. View-only'). This does not block handoff, but the §10 component description should be updated to reflect the v2.0 Phase A/B/C scope."
  - id: "WARN-002"
    type: "design-system"
    source: "docs/specs/front/design-system/components.md"
    description: "design-system/components.md changelog still shows version 1.3.0 (updated for progressive-disclosure wave), but the version header at the top of the file still reads '1.3.0' while the index (_index.md) changelog says v1.4.0 was added for this wave — minor version mismatch between the two changelog entries. No blocking content divergence detected."
