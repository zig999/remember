validation:
  domain: chat
  artifact_version: "2.4.0"
  mode: incremental_back
  validated_by: u-spec-validator
  timestamp: "2026-06-22T20:38:00Z"
  layer: permanent

status: VALID
blocking_count: 0
warning_count: 1
handoff_allowed: true

warnings:
  - id: WARN-001
    type: cross-ref
    source: docs/specs/domains/ingestion/ingestion.spec.md §7 Cross-Domain Dependencies
    description: >
      chat domain is NOT listed in ingestion.spec.md §7 as a consumer, despite
      bidirectionality requirement and chat.spec.md §7 listing ingestion as a service-level
      consumer (BR-43: ingestRawInformation + runLlmExtraction). The v1.3.0 changelog for
      ingestion.spec.md added UC-13 referencing the chat module but did not update the formal
      §7 dependency table. Warning-level only; does not block handoff.

resolution_notes: >
  Correction applied 2026-06-22: BUSINESS_CHAT_INGEST_DISABLED has been added to
  docs/specs/_global/error-codes.md Chat section (after BUSINESS_TURN_IN_PROGRESS) with
  HTTP 503 status. The original INVALID result was correct at time of validation; this
  correction reflects the spec fix that was applied in response to the validator's finding.
