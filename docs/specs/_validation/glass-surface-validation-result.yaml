# Validation result — machine-readable companion to glass-surface-validation.md
# Generated: 2026-06-19T00:03:30Z
# Overwritten on every validation run — always reflects the most recent state.

schema_version: "1.0"
domain: glass-surface
validation_run:
  date: "2026-06-19"
  mode: "component-spec"
  task_id: "sdd_glass-surface_spec-validator"
  attempt: 1

status: VALID
handoff_allowed: true

blocking_count: 0
warning_count: 1

artifacts_validated:
  - path: "docs/specs/front/components/GlassSurface.component.spec.md"
    version: "1.1.0"
    status: "approved"
  - path: "docs/specs/front/components/GlassSurface.back.md"
    version: "1.0.0"
    status: "draft"
  - path: "docs/specs/front/design-system/tokens.md"
    version: "1.0.0"
    status: "draft"
    role: "cross-reference"

checks:
  - id: "check-1"
    description: "Component spec and back.md internal consistency"
    result: PASS
  - id: "check-2"
    description: "All token references in component spec resolve in tokens.md"
    result: PASS
    tokens_checked: 28
    tokens_resolved: 28
  - id: "check-3"
    description: "Dual border namespace pattern consistently demonstrated in BOTH files"
    result: PASS
  - id: "check-4"
    description: "Motion variants match between spec, back.md, and tokens.md"
    result: PASS
    note: "W-01 warning — implementation mechanism drift (CSS keyframe vs implied Framer Motion)"
  - id: "check-5"
    description: "All 3 glass levels covered in both documents"
    result: PASS
    levels_covered: ["ambient", "panel", "modal"]
  - id: "check-6"
    description: "WCAG 2.2 AA requirements consistent"
    result: PASS
  - id: "check-7"
    description: "File paths in back.md are valid relative paths"
    result: PASS

inconsistencies:
  blocking: []
  warnings:
    - id: "W-01"
      type: "spec-implementation-drift"
      source_file: "docs/specs/front/components/GlassSurface.component.spec.md"
      source_section: "§8, §4"
      target_file: "docs/specs/front/components/GlassSurface.back.md"
      target_section: "§7.2"
      description: >
        Spec §8 and §4 state table imply the uncertain pulse uses a Framer Motion variant
        (motion.pulse.uncertain) scoped to border-color. Back.md §7.2 supersedes this with
        CSS @keyframes in theme.css, explicitly marking the Framer variant as not shipped.
        User-observable behavior is identical; back.md is authoritative on mechanism.
      suggested_fix: >
        Update GlassSurface.component.spec.md §8 to note that the uncertain pulse is
        implemented as a CSS @keyframes animation (data-glass-pulse="uncertain" attribute)
        rather than a Framer Motion variant.
      responsible_agent: "Front Spec Agent"
      severity: warning

report_path: "docs/specs/_validation/glass-surface-validation.md"
