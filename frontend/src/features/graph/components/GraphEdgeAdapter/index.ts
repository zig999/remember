/**
 * GraphEdgeAdapter — public surface (per-component barrel; front.md §6.4).
 */
export { GraphEdgeAdapter } from "./GraphEdgeAdapter";
export type {
  GraphEdge,
  GraphEdgeAdapterProps,
  GraphLinkDataRF,
} from "./GraphEdgeAdapter.types";
