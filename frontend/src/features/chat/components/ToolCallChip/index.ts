/**
 * ToolCallChip — per-component barrel (allowed exception, CLAUDE.md §Component
 * contract: a per-component `index.ts` re-exporting the public surface is
 * permitted; project-wide `export *` barrels remain forbidden).
 */
export { ToolCallChip } from "./ToolCallChip";
export type { ToolCallChipProps } from "./ToolCallChip.types";
