/**
 * StateBadge — public surface (front.md §6.4, per-component index.ts allowed).
 */
export { StateBadge, stateBadgeVariants } from "./StateBadge";
export type { StateBadgeVariants } from "./StateBadge";
export type {
  ConfidenceState,
  StateBadgeProps,
  StateBadgeSize,
} from "./StateBadge.types";
