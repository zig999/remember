/**
 * Popover — public surface (per-component barrel; front.md §6.4).
 */
export { Popover, PopoverTrigger, PopoverAnchor, PopoverContent } from "./popover";
export type { PopoverContentProps } from "./popover.types";
